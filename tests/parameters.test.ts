import {describe,expect,it} from 'vitest';import {defaults,qualityFilaments} from '../src/math/parameters';
describe('parameter preset',()=>{it('starts as a quiet bilateral field',()=>{expect(defaults.c).toBeGreaterThan(2);expect(defaults.r).toBeGreaterThan(1.5);expect(defaults.duration).toBe(34);expect(defaults.diamondHoldDuration).toBe(4);expect(defaults.diamondSpectralFadeDuration).toBe(1);expect(defaults.progress).toBe(0);});it('has quality tiers and a growing line budget',()=>{expect(qualityFilaments.low).toBeLessThan(qualityFilaments.high);expect(defaults.peakLines).toBeGreaterThan(defaults.initialLines);});it('fits the spectral schedule',()=>{expect(defaults.diamondCompleteTime+defaults.diamondHoldDuration+defaults.spectralEmergenceDuration+defaults.spectralExpansionDuration+defaults.ecstaticHoldDuration).toBe(defaults.duration);});it('uses a vivid chromatic spectral pipeline',()=>{expect(defaults.spectralSaturation).toBeGreaterThan(1);expect(defaults.spectralEmissionIntensity).toBeGreaterThan(2);expect(defaults.overlapWhiteningThreshold).toBeGreaterThan(2);});});
